All saliency_ds files are generated using new parameters.
Currently, not cut-off is used in the preprocessing.
The ground truth saliency maps  are generated from the colfax server, then copied down to this local server
ds_dcnn_step01 means the saliency dataset generated by dcnn model having 1 seconds steps. Similarly, 003 means 0.3 seconds
